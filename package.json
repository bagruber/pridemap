{
  "name": "pridemap",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && node scripts/inject-jsonld.mjs",
    "build:colorful": "vite build --mode colorful && node scripts/inject-jsonld.mjs",
    "preview": "vite preview",
    "deploy": "npm run build:colorful && gh-pages -d dist",
    "og": "node scripts/og-images.mjs",
    "og:force": "node scripts/og-images.mjs --force",
    "build:hostinger": "npm run og && vite build --base=/ && node scripts/prerender.mjs",
    "deploy:hostinger": "npm run build:hostinger && gh-pages -d dist -b hostinger",
    "prepare-data": "node scripts/prepare-data.js"
  },
  "dependencies": {
    "@fontsource/sofia-sans": "^5.2.8",
    "@here/flexpolyline": "^0.1.0",
    "flag-icons": "^7.5.0",
    "lucide-react": "^1.16.0",
    "maplibre-gl": "^5.0.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-icons": "^5.6.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "gh-pages": "^6.3.0",
    "playwright-core": "^1.61.1",
    "vite": "^6.0.5"
  }
}
